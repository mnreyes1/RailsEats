<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :hora_entrega %><br>
    <%= form.time_select :hora_entrega %>
  </div>

  <div class="field">
    <%= form.hidden_field :estado, value: "pedido tomado por restaurante" %>
  </div>

  <div class="actions">
  <% if current_restaurante %>
    <%= form.submit "Tomar pedido", class: "btn btn-success" %>
<% else %>
    <%= form.submit "Editar Orden", class: "btn btn-success" %>
<% end %>
  </div>
<% end %>
